{
  "name": "clipmd",
  "version": "1.0.4",
  "description": "一键剪藏全网内容为纯净 Markdown，自动同步至你的知识库，让每一次浏览都沉淀为可检索的私人知识资产.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "npm run typecheck && npm test && npm run build && node scripts/verify-market-build.mjs"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "esbuild": "0.17.3",
    "jsdom": "^26.1.0",
    "obsidian": "latest",
    "typescript": "^5.0.2",
    "vitest": "^2.1.9"
  }
}
